@page
@model Identity.API.Pages.Account.ResetPasswordConfirmation.Index

<div class="login-page">
    <div class="lead">
        <h1 class="text-center">Reset password confirmation</h1>
        <p class="text-center">We have reset your password</p>
    </div>

    <div class="row d-flex justify-content-center">
        <div class="col-sm-6">
            <div class="card">
                <div class="card-header">
                    <h2>Reset password confirmation</h2>
                </div>

                <div class="card-body">
                    @if (ViewContext.ViewData.ModelState.ErrorCount > 0)
                    {
                        @Html.ValidationSummary(false, null,  new { @class = "text-danger" })
                    }
                    <div>
                        <p class="mt-3">
                            Your password has been reset. Please click <a href="@Model!.Input!.ReturnUrl">here</a> to login.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
